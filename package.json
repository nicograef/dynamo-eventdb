{
  "name": "@nicograef/dynamo-eventdb",
  "version": "0.0.0",
  "description": "An event-sourcing database wrapper for DynamoDB.",
  "license": "MIT",
  "type": "module",
  "exports": "./dist/src/index.js",
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint --fix --max-warnings=0 './src/**/*.{ts,js}'",
    "prepare": "husky",
    "format": "prettier --write './**/*.{json,md,ts,js}'",
    "test": "vitest run --config vitest.config.ts",
    "test:integration": "pnpm exec vitest run --config vitest.config.integration.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "@types/node": "^24.5.2",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.36.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.44.0",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "@aws-sdk/client-dynamodb": "^3.879.0",
    "@aws-sdk/lib-dynamodb": "^3.879.0",
    "typescript": "^5.9.0",
    "zod": "^3.25.71"
  },
  "engines": {
    "node": ">= 22.0.0",
    "pnpm": ">= 10.0.0"
  }
}
